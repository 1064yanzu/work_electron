# 2026-08-12

## 精简批次 0（A1–A5）完成

按 `docs/代码分析/精简决策方案.md` 执行批次 0 零风险清理：

- 删 `sync:open-design*` 失效 scripts、`pnpm-lock.yaml`、依赖 `class-variance-authority`。
- 新增 DB schema v8（`electron/main/db/migrations/v8Migrations.ts`）DROP 孤儿表
  `workflow_nodes` / `workflow_run_logs`；initSql 不再建表；备份清单剔除；
  连带删前端死封装 `src/lib/api/workflow.ts` 及 types.ts workflow 类型。
- `cherry-studio参考/`（725MB）+ `.references/`（62MB）移至仓库外
  `/Volumes/external disk/develop/_reference-archives/`。
- 验证：typecheck 绿；dev 冒烟正常；真实库 user_version=8、workflow 表已 DROP。
- 施工文档：`docs/施工文档-精简批次0-零风险清理.md`；changelog 已追加。

## 环境坑位（重要）

- 本机 agent shell 被宿主注入 `ELECTRON_RUN_AS_NODE=1`，直接 `npm run dev` 会让
  Electron 以纯 Node 运行报 `does not provide an export named 'BrowserWindow'`。
  冒烟需 `env -u ELECTRON_RUN_AS_NODE npm run dev`。
- 数据库位置：`~/Library/Application Support/ipo-workbench/db.sqlite`，
  可用 `/usr/bin/sqlite3` 直接验证 `PRAGMA user_version`。

## 待续

- 批次 1（B 节）：26 个零引用死代码候选逐个复核后删除（InputDock / ToolPermissionCard /
  SourceManager / WebdavBackupManager / ToolCallTrace 等，约 5k 行）。
